

## Dashboard create page (app/dashboard/page.tsx) — media features
- MOODS is now an array of `{name, img}` objects; chips show a small 28px circular avatar (images in /public/moods/*.jpg, 8 abstract gold/purple mood thumbnails).
- VOICES include `audio` mp3 paths (/public/voices/*.mp3) generated via edge-tts (Jenny/Guy/Ava/Andrew neural). Each voice card has a play/pause button overlay; only one plays at a time (managed by audioRefs + playingVoice state). Soundbar animation `animate-soundbar` defined in globals.css.
- "See It In Action" section uses EXAMPLE_REELS array of real 9:16 MP4s in /public/examples/ (abundance/wealth/peace/power .mp4 + matching -poster.jpg). Built locally with ffmpeg (Ken Burns zoom + drawtext affirmation + voice audio). Tap to play with sound (videoRefs + playingReel state, one at a time).
- Tooling note: VM has ffmpeg + python; install edge-tts via pip for TTS. bc is NOT available (use awk for math). Asset subtask saves images to CDN but NOT to local project paths — must curl them down to /public/... manually.
