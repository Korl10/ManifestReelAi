## Phase 3 — Music Library Integration (COMPLETE + LIVE-PROVEN on dev, NOT yet deployed)
- **3.1 Enrichment**: computed BPM for all 45 V1 tracks via librosa (octave-normalized to 65-150; 8 corrected). Added `bpm`, `size_kb`, `license_status` to `public/music-library/manifest.json` (version bumped 1.1). All 45 = `royalty_free` (owner's own Suno exports). `MusicTrack` interface (lib/music-library.ts) + `normalizeTrack` + `dbTrackToMusicTrack` updated with `size_kb` + `license_status`.
- **3.4 Schema (additive, pushed)**: LibraryTrack got `sizeKb Int?` + `licenseStatus String @default("royalty_free")`. New `MusicFavorite` model {userId,trackId,@@unique}. User got `musicFavorites` relation. 80 LibraryTrack rows preserved.
- **3.2/3.3 UI**: `musicCapabilities(subTier,isTrialing)` helper in lib/model-tiers.ts → {canBrowse,canRegenerate,canFavorite,canBulkLicense,lockedReason}. Free/Trial=locked (no browse), Starter=browse, Pro=+regenerate, Premium=+favorite, Agency=+bulk license CSV. New `/api/music/library` (suggested top5 + full sorted list + favorites + caps) and `/api/music/favorites` (GET/POST/DELETE, Premium+ gated). `components/music-picker.tsx` rewritten: "🎯 AI Suggested for [Mood]" section, per-card mood/energy/BPM/duration badges, regenerate button (Pro+), favorite star (Premium+), browse accordion (Starter+), bulk license CSV (Agency).
- **3.4 Attribution**: `/api/reels/[id]` GET now returns `music:{trackId,title,licenseStatus,attributionRequired}` via resolveReelMusic helper. Reel detail page shows a "Music credit" box ONLY when attributionRequired (never burned into video). Admin music-loader page shows color-coded license badge column (green=royalty_free, amber=attribution, sky=paid, red=review/unknown).
- **3.5 BONUS has_vocals ducking**: `CompositeInput.musicHasVocals?` threaded from generation-pipeline (`musicHasVocals = matched.has_vocals`). Compositor voice+music branch adapts: instrumental → bed 0.40/ratio10/release300; vocal → bed 0.22/ratio20/thresh0.015/release450 (≈12dB harder duck). All 45 tracks currently has_vocals=false (future-proofing).
- **Files changed Phase 3**: manifest.json, lib/music-library.ts, lib/model-tiers.ts, prisma/schema.prisma, lib/compositor.ts, lib/generation-pipeline.ts, components/music-picker.tsx, app/api/music/library/route.ts (NEW), app/api/music/favorites/route.ts (NEW), app/api/reels/[id]/route.ts, app/dashboard/reel/[reelId]/page.tsx, app/admin/music-loader/page.tsx.

## CRITICAL FIX — Music silent-tail bug (compositor) [fixed, NOT yet deployed]
- **Symptom found during Phase 3 audio QA**: every reel ended in DEAD SILENCE for 1.6–5.7s. Audio stream length ≈ VO length, NOT the target reel length (e.g. Reel2: 15s video but audio only 9.29s = 5.7s silent tail).
- **Root cause**: in lib/compositor.ts voice+music branch, `sidechaincompress` only emits output while BOTH inputs have signal, so the ducked music `[mduck]` ended exactly when the VO (sidechain key) ended. When the pipeline extends the final scene past the VO to hit target duration T, the music died with the VO → silent video tail.
- **Fix**: pad the sidechain key with silence to T before sidechaincompress: `[voice]...asplit=2[vo][vokeyraw]; [vokeyraw]apad,atrim=0:T,asetpts=PTS-STARTPTS[vokey];` then `[musbed][vokey]sidechaincompress...[mduck]; [mduck]atrim=0:T,...afade...[musf]`. Once VO stops, key is silent → no compression → music gently swells back to bed level and sustains under the held outro frame, then fades over last 0.8s. Also padded the voice-only branch (`apad,atrim=0:T`).
- **PROOF (3 regenerated reels, all audio == video length)**: Reel1 15.0/15.0, Reel2 15.0/15.0 (was 9.29), Reel3 25.0/25.0. Outro music (VO absent) now audible: R1 −30.0dB mean/−19.3 peak, R2 −23.1/−12.2, R3 −25.9/−11.0; final fades −33 to −41dB (no hard cut). VO peaks −4.8 to −10dB sit clearly above ducked bed.
- **Music auto-match deterministic per mood/style/platform**: grateful→Sacred Sunrise(90bpm,low), empowered→Warrior Pulse(120bpm,high,V2 id v2_...), joyful→Open Skies(95bpm,mid). Matcher only returns has_vocals=false tracks, so musicHasVocals is currently always false in auto-match (aggressive vocal ducking path untested live).
- Checkpoint saved: "Fix music silent-tail bug in compositor". Library card text white/60→white/70. AWAITING USER AUDIO APPROVAL before deploying Phase 3 + this fix to both hosts.

## REVISED STANDARD TIER + TWO LIVE-TEST BUGS FOUND & FIXED (checkpointed, NOT deployed)
- **Standard tier REVISED (folded in + LIVE-PROVEN)**: Standard now = **2 cinematic motion scenes (Kling 2.5 Turbo) + Flux 1.1 Pro stills** (was "stills-only"). Code: lib/generation-pipeline.ts `tierAllowsMotion` includes 'standard'; `maxMotion = cinematic?sceneCount:(standard?2:4)`. lib/model-tiers.ts Standard tagline/features/tierModelSummary updated. landing-page.tsx Cinematic desc mentions Standard 2 motion. LIVE PROOF (SMOKE3-Std15): ready, 15.01s, 2/2 Kling motion verified, MP4 https://cdn.abacus.ai/videos/6c568218-84ea-430f-a114-e3dd6eb16bc9.mp4, $0.75. Grandfather existing Standard payers 90 days (billing policy, no code).
- **BUG #1 (FIXED+VERIFIED) — Whisper hallucination poisoned duration**: pipeline blindly replaced ElevenLabs char-aligned word timestamps with Whisper re-transcription; Whisper Large v3 sometimes hallucinates a long tail (28.2s words for 9.0s audio) → compositor padded video to bogus length (27s video for 15s target → duration hard-fail). TIER-INDEPENDENT. Fix (lib/generation-pipeline.ts ~524-555): only adopt Whisper words if `abs(whisperDur-elDur) <= elDur*0.25+1.5`, else keep ElevenLabs (`elDur=v.durationSec` = ground truth). Verified: all post-fix reels hit perfect durations.
- **BUG #2 (FIX NOW LIVE-VERIFIED ✅) — Veo3 transient 422 misclassified as permanent**: RE-VERIFIED on a 25s cinematic reel (VERIFY25-Luxury, reel cmq807u0o0001sunutcwrlgko): status=ready, **25.00s (delta 0)**, **6/6 Veo3 motion verified=true**, MP4 https://cdn.abacus.ai/videos/8dab2432-bcfe-443e-856f-69611b1d6397.mp4, thumb https://cdn.abacus.ai/images/76783351-2e91-4550-b8c4-1cdad9a28b6c.jpg, cost $12.48. During the run, **3 transient Veo3 422 "model did not generate" misses occurred and ALL were recovered via the retry path** (attempt 1/4, 1/4, 2/4 → resubmit → success) → 6 CLIP COMPLETE. Whisper adopted within tolerance (whisperDur=17.05 vs elDur=17.37 → Bug#1 guard active, no hallucination). narration=17.05s fits 25s, narrationFits=true, no dead air/cutoff. ⚠️ KEY OPS FINDING: 25s cinematic = 6 SEQUENTIAL Veo3 clips (perReelConcurrency=1); under congestion w/ repeated transient misses, wall-clock can exceed 15min (this run took ~17min incl. recovery). The $9/reel cap is UNREALISTIC for 25s cinematic — video alone = 6×$2 = $12. 15s cinematic (4 clips) ≈ $8.34. Recommend raising per-reel timeout to 20-25min for 25s cinematic, or raise QUEUE_LIMIT_VEO3/allow 2 concurrent submits per reel to cut wall-clock.\n- **ORIGINAL fix detail**: Veo3 frequently returns 422 "model did not generate the expected output / may occur for several reasons" = TRANSIENT. lib/providers/falai-video.ts pollClip treated ALL 422 as permanent content-policy block. But Veo3 frequently returns 422 "model did not generate the expected output / may occur for several reasons" = TRANSIENT miss that succeeds on retry. Caused Cine25-A to get only 3/6 clips → all-or-fail cinematic guardrail correctly hard-failed+refunded (#1 cause of cinematic fails). Fix (falai-video.ts ~144-174): distinguish `isPolicy` (content_policy/safety/flagged/prohibited/violat/nsfw/"not allowed"→PermanentError) from `isTransientGenMiss` ("did not generate the expected output"/"may occur for several reasons"/"please try again"→RetryableError → bubbles to 4-attempt resubmit loop). Unknown 422 → brief fetch retries then retryable.
- **LIVE TEST 1 results (partial, pre-Bug#2-fix)**: Cine15-A ready 15.00s 4/4 Veo3 $8.34; Cine15-B ready 15.00s 4/4 $8.33; Cine15-C ready 15.00s 4/4 $8.34; Cine25-A FAILED 3/6 (Bug#2, refunded $0). MP4s: fa784640.../3b300974.../0646858f... on cdn.abacus.ai. Cinematic 15s=4 scenes ≈$8.34; 25s=6 scenes ≈$12-15.
- **TEST HARNESS — HARDENED & WORKING ✅**: ROOT CAUSE of prior detached-process deaths: the launching bash call inherited the child's stdout pipe, so it BLOCKED until the 120s tool timeout, then got SIGKILL'd taking the child with it. FIX: `scripts/launch.sh <logfile> <command...>` uses `setsid bash -c 'exec CMD >> log 2>&1' </dev/null & disown` and RETURNS INSTANTLY (echoes pid, exit 0) so the tool call completes in <1s and nothing is killed. `scripts/run-reel-hardened.ts <tier> <dur> <label> <resultsFile> [prompt] [mood] [style] [voice]` adds: heartbeat log every 30s, per-reel hard timeout via Promise.race (env REEL_TIMEOUT_MS default 900000), graceful skip on timeout (no cascade), appends RESULT json + writes `<resultsFile>.done` sentinel, captures perScene/whisper/thumbnailUrl. Use `env VAR=val` prefix INSIDE the command (exec rejects bash assignment prefixes). Poll across calls by sleep+grep log + check `.done`. ⚠️ Even after harness timeout writes 'timeout' RESULT, the dangling runGenerationPipeline promise KEEPS RUNNING and may finish the reel (VERIFY25 finished 6/6 ~2min after the 15min cap) — re-check DB status after a timeout before concluding fail. RAISE REEL_TIMEOUT_MS to 1500000 (25min) for 25s cinematic. Spend so far ≈ $46.\n- **TEST HARNESS LEGACY**: scripts/run-reel.ts, scripts/run-batch.ts (reads scripts/batch-configs.json), scripts/_status.ts <reelId>, scripts/_check.ts. Call runGenerationPipeline(jobId,reelId,userId,'full',{modelTier,targetDuration}) directly; create Reel(status:rendering,motion:true,coinCost:0,tier)+GenerationJob(status:queued) first.

## Motion Reliability + Duration Discipline (Fix A/B/C/D) — CODE COMPLETE + UNIT-VERIFIED, LIVE TEST PENDING, NOT DEPLOYED
- **Trigger**: paying owner caught (1) reels not filling requested duration (held-frame dead zones) + (2) "cinematic" reels were image slideshows not motion. Audit: 68% motion failure = fal.ai rate-limit cascade from NO retry/backoff; 13/15 hard-fails were cinematic (Veo3) in one 31-min burst; "ready" cinematic reels only had 3-4/6 scenes animated (broken ready gate).
- **Fix B1 retry/backoff** (lib/providers/falai-video.ts, REWRITTEN): RetryableError/PermanentError classes; BACKOFF_MS=[2,4,8,16]s +0-1s jitter; submit retries 429/5xx, poll retries timeout/FAILED, 422/content_policy = permanent; attemptClip = 4-attempt backoff loop; generateOneClip = +1 end-to-end retry; release() idempotent (safe double-release in catch+finally).
- **Fix B2 global queue** (lib/concurrency/model-queue.ts, NEW): in-process per-family semaphore singleton on globalThis. Limits env-configurable QUEUE_LIMIT_VEO3=2/KLING=4/FLUX=6. acquire() FIFO with direct slot hand-off (releaser increments active for next waiter — NO over-subscription; unit-tested: 20-task burst caps at 2). previewWait()→{position,etaMs}. stats()→per-family {active,queued,avgTaskSec,throughputPerHour,estWaitSec}. Pipeline sets job.currentStep="Rendering motion — position N in queue, ETA ~Xs" before motion; progress UI renders it. Admin dashboard `/admin/ops` (app/admin/ops/page.tsx + app/api/admin/ops/route.ts, admin-gated) auto-refresh 5s. Ops tab added to admin-shell.
- **Fix B3 per-reel throttle**: generate() perReelConcurrency = Veo3?1:2 + INTER_SUBMIT_DELAY_MS=1200 stagger.
- **Fix B4 cinematic all-motion guarantee + UX**: motion verify cinematic requires clips>=motionExpected(=all scenes), pro requires >0. Removed silent cinematic→Kling downgrade. Hard-fail+refund whole reel if not met; errorMessage "N scenes couldn't be animated (Veo 3 congestion)...". scenesJson stores model_tier+targetDuration+failureReason. NEW retry endpoint app/api/reels/[id]/retry (mode 'same'|'switch_pro'→pro/Kling; re-gates+re-charges, fires fresh pipeline). Progress page shows CTAs [Retry on Veo 3][Switch to Kling 2.5 (Pro)][Try again later] on motion failures.
- **Fix B5 ready gate**: tierAllowsMotion = pro||cinematic. STANDARD IS NOW STILLS-ONLY (no motion) — wantsMotion requires tierAllowsMotion. ⚠ BEHAVIOR CHANGE flagged to user.
- **Fix A duration discipline**: llm-script.ts budget = (target-0.8)/0.55*spd (was target-2.0/2.5). enforceDurationTarget UNDER-branch DISTRIBUTES residual across all scenes (PER_SCENE_CAP=1.2, FINAL_SCENE_CAP=1.5, proportional fill). compositor.ts Ken Burns continuous drift (rate 0.0006/frame, cap 1.30, never caps within scene) + ANIMATED parallax pan via output frame `on` (kills slideshow feel). Runtime assert in pipeline (~line 671): narrationDuration(last word end)<=finalDuration+0.5 AND finalDuration<=targetDur+0.3 → hard-fail+refund (failureReason narration_overrun/duration_overshoot). Closes Mode-2 voice-cutoff bug.
- **Fix D transparency**: lib/model-tiers.ts features rewritten (Standard="Flux 1.1 Pro AI stills"+"Cinematic Ken Burns motion" NOT Kling; Cinematic="every scene animated"+"100% all-motion"). tierModelSummary() helper. model-tier-picker.tsx shows "This reel will use: ..." line. Admin reels page added Model column (from scenesJson.model_tier). Landing page feature: "Cinematic AI Motion ... powered by Veo 3 Fast".
- **Checkpoint saved**: "Fix A+B+C+D: motion reliability + duration discipline". Build + tsc clean.
- **STILL PENDING (expensive, hit cost ceiling mid-session)**: 12-reel sequential live test (Cine15×3,Cine25×3,Pro15×2,Pro25×2,Std15×1,Std25×1) + 6-reel concurrency test + MP4 proof links + spend log. ⛔ NO DEPLOY until user reviews live MP4 proof.
- **Test accounts**: john@doe.com (premium+admin, userId cmpya5two0000ya8tc39di0va, 100 credits) for cinematic. demo@manifestreel.ai(pro). freetest@manifestreel.ai(free).

## Phase 5 — Pricing/Credits/Anti-abuse (IN PROGRESS)

### Architecture: PRICING_V2 parallel module behind feature flag
- `lib/pricing-v2.ts` is the NEW canonical credit-based system, built in PARALLEL to the legacy coin system (`lib/pricing.ts`). Gated by `process.env.PRICING_V2 === 'true'` (`PRICING_V2_ENABLED`). Flag is OFF until migration runs (Phase 5F). Do NOT rip out legacy coin code until flag flips + 48h soak.
- Canonical tier keys: starter | creator | pro | studio. Legacy DB tier strings: starter | pro | premium | agency. `legacyTierMap` maps legacy→canonical for migration: {starter→starter, pro→creator, premium→pro, agency→studio}.
- Grandfathered users keep OLD label+price via `LEGACY_LABELS`; new FEATURE matrix applies to all.

### Phase 5A — DONE & checkpointed (NOT deployed)
- `lib/pricing-v2.ts`: PLANS_V2 {starter $14.99/1500cr/10voices/standard, creator $34.99/4000cr/30voices/pro, pro $79.99/10000cr/151voices/cinematic, studio $199/30000cr/151voices+cloning}. Annual = 40% off (Starter $107.88, Creator $251.88, Pro $575.88, Studio $1432.80).
- Credit engine: CREDIT_BASE_COSTS {static:15,standard:30,pro:60,cinematic:150} × DURATION_MULTIPLIER {5:1,10:2,15:3,25:5,30:6} + ADDONS {premiumVoice:5,premiumMusic:5,upscale4k:30,craftPreset:0}. `reelCreditCost(quality,dur,addons)`, `reelCreditBreakdown(...)`. Verified: 15s Pro = 180 ✓.
- TOPUP_PACKS: 1000/$14.99, 3000/$39.99(popular), 8000/$99.99, 20000/$199.
- Coin→Credit: `coinsToCredits(coins) = ceil(coins*2.7/10)*10`. COIN_TO_CREDIT_RATE=2.7. CONVERSION_BANNER text.
- Voice gating: STARTER_VOICE_IDS (10), CREATOR_VOICE_IDS (20 more), rest=pro. `voiceTierFor(id)`, `voiceAccess(planTier,id)→{locked,requiredTier,requiredTierName}`, `planAllowsQuality()`. Derived from voiceId — did NOT modify voice-catalog.ts.
- UI: `components/voice-browser.tsx` got optional props `planTier?: PlanTierV2` + `onUpgrade?`. Locked voices show 🔒[Tier] badge, dimmed, click→upgrade modal (or onUpgrade cb); preview still plays for all. `components/conversion-banner.tsx` (display-only). Internal preview page `app/dev/pricing-v2/page.tsx` (NOT in nav) renders plans/cost-samples/topups/conversion/gated-picker — used for verification screenshots.

### Phase 5 remaining (sequential, await user greenlight per phase):
- 5B: Gate1 email OTP + disposable-domain blocklist; Gate2 Stripe card-fingerprint lifetime lock; device fp via open-source FingerprintJS (no key). Trial tightening: 5s/720p/Standard-only/1-reel-hard-limit/ASS watermark.
- 5C: Stripe — build 4 products×monthly/annual + 4 topup prices via API, report IDs. MUST call get_implementation_guidelines topic STRIPE first. Webhook via SIMULATION (no Stripe CLI in hosted env).
- 5D: reCAPTCHA v3 (user provides keys; stub w/ feature flag now), IPQualityScore swappable interface (start with free ip-api.com/ipapi.co).
- 5E: admin /admin/conversions + /admin/abuse pages.
- 5F: coin→credit DB migration (DRY-RUN first, report deltas), set is_grandfathered, flip PRICING_V2=true.
- Email (5B+): 5 trial-drip templates via platform built-in notifications (no Resend).
- DB already has: Subscription.{trialUsed,trialReelsUsed,trialEndsAt,cardFingerprint,graceEndsAt,introUsed}. NO TrialLock/Voice models yet.
