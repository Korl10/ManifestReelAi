# ManifestReel AI — Project Memory

## Overview
AI vertical-video ("reel") generator for manifestation/spiritual creators. Cost-conscious founder; cares about unit economics & margin dashboard. Theme: gold #D4AF37 + royal purple #7B2FBE/#A855F7, dark. CSS classes: `gold-gradient`, `gold-glow`. Never reveal framework names to user ("your app").

## Deployment
Two untagged hostnames, deploy each SEPARATELY (2 calls, NO deployment_tag): `manifest-reel-ai-qcjkqa.abacusai.app` and `manifestreelai.com`.

## Accounts
- Admin/Premium test (hidden from user): john@doe.com / johndoe123
- Demo/Pro: demo@manifestreel.ai / demo123456
- Existing paying users (preserve coin economy): Pro `cmq0djw1q0007pd081pprhoal`, Premium `cmq0d0mbz0000pd08tgbxrd1h`

## Pricing (current)
Pro $19.99→30 coins; Premium $49.99→60 coins. Static reel = 1 coin. Bundles: starter 10/$7.99, creator 25/$16.99, pro-pack 60/$34.99, studio 150/$74.99.
Phase 5 (future): credits overhaul 1cr=$0.10; Starter $29/300, Creator $79/1000, Pro $179/2500, Studio $499/8000; grandfather existing.

## Pipeline / infra
- `lib/generation-pipeline.ts` orchestrates: script → images → (motion) → voice → subtitles → music → composite.
- Image gen: Abacus image-gen.ts (gpt-5.4, 1024x1536) + Flux (fal) for motion tiers.
- Voice: ElevenLabs (lib/voice-catalog.ts 57 voices; tiers flash/multilingual/turbo). components/voice-browser.tsx, subtitle-editor.tsx (dynamic ssr:false).
- Compositor: FFmpeg via Abacus API, filter_complex MUST be double-quoted, 1080x1920@30fps.
- fal.ai content policy: avoid divine/sacred/spiritual/temple/god/angel (422). Motion uses camera-only templates (motion-prompts.ts). Flux uses enable_safety_checker:false + safety_tolerance 5.
- .env: STRIPE_SECRET_KEY, FAL_KEY (key_id:key_secret), ELEVENLABS_API_KEY, ABACUSAI_API_KEY.
- Run tsx: `cd nextjs_space && set -a && . ./.env && set +a && node_modules/.bin/tsx file.ts`

## Phases
- P1 + P1.5 + P2 (subtitles + voice browser) DONE & deployed.
- **P3 (Music + Model Tiers + Cost dashboard) DONE & deployed.**
- **P4a (Craft brand presets — core CRUD + apply to reel) DONE & deployed.**
- P4b (next): tier-gating acceptance test + analytics (most-used preset + admin "Top presets" widget).
- P4c (next): watermark render pipeline (logo overlay on final MP4 at position/opacity/size).
- P5 (future): pricing/credits overhaul.
- P6 (future): team/shared presets + AI preset gen (OUT OF SCOPE for P4).

## Phase 4a details (DONE)
### Craft = reusable brand presets ("/dashboard/craft", Palette icon in sidebar nav)
- **Tier limits (`brandPresetLimit` in lib/brand-presets.ts):** free→0, pro→1, premium→-1 (unlimited). `-1`==unlimited. presetLimitLabel() for upgrade copy.
- **DB:** `BrandPreset` model (table `brand_presets`) + `User.brandPresets` relation + `Reel.brandPresetId String?` (onDelete:SetNull) + `@@index([brandPresetId])`. Fields: name, isDefault, usageCount, logoPath/logoUrl, primaryColor(#D4AF37)/accentColor(#7B2FBE), watermark{Show,Position,Opacity 10-100,Size S/M/L}, subtitleStyle(Json full SubtitleStyle), voice{Id,Stability,Similarity,Tier,clonedVoiceId}, music{Moods String[],Styles String[],stingerEnabled,lockedTrackId}, modelTier/aspectRatio/visualKeywords/motionDefault, default{Platform reels|tiktok|shorts|youtube, Length 15|25|30}, ctaText.
- **lib/brand-presets.ts** (CLIENT-SAFE): types + consts (WATERMARK_POSITIONS/SIZES, ASPECT_RATIOS, PRESET_PLATFORMS, REEL_LENGTHS, PRESET_MUSIC_MOODS=7 library moods), emptyPreset(), brandPresetLimit/isUnlimited/presetLimitLabel.
- **lib/brand-presets-server.ts**: rowToPreset(row) (merges subtitleStyle Json w/ DEFAULT_SUBTITLE_STYLE, ISO dates), sanitizePresetInput(body) (validates hex/clamps/enums; EXCLUDES isDefault — default set via dedicated route only).
- **API:** /api/presets GET(list+{tier,limit,unlimited,used,canCreate,limitLabel}) POST(create, tier-gated 403 reason:'preset_limit', first preset auto-default, txn unsets others). /api/presets/[id] GET/PATCH/DELETE (DELETE promotes next to default). /api/presets/[id]/default POST. /api/presets/[id]/duplicate POST (tier-gated, name+" (Copy)"). /api/presets/presign POST (logo upload, gated to paid tiers, png/svg/jpeg/webp, returns {uploadUrl,cloud_storage_path,publicUrl}).
- **UI:** `app/dashboard/craft/page.tsx` = card grid (color stripe primary→accent, logo/fallback, default pin badge, meta chips, Use/Default/Edit/Duplicate/Delete actions) + tier banner + empty state + UpgradeModal + ConfirmDelete. `app/dashboard/craft/_components/preset-wizard.tsx` = 4-step modal (Identity→Voice&Music→Visual→Subtitles) reusing VoiceBrowser/ModelTierPicker/MusicPicker/SubtitleEditor (all dynamic ssr:false). Logo upload: presign→PUT w/ Content-Type. Save: POST(new)/PATCH(edit).
- **Apply to reel:** dashboard reads `?preset=ID` (window.location in useEffect, NO useSearchParams to avoid Suspense build issue) → GET /api/presets/[id] → applyPreset() prefills platform(slug→label map)/voice/voiceTier/stability/similarity/subtitleStyle/modelTier/enableMotion(motionDefault)/enableStinger/musicTrackId(lockedTrackId). Banner "Using preset: X" w/ Clear (history.replaceState strips param). handleGenerate sends `brandPresetId`. generate route validates ownership → stores reel.brandPresetId + best-effort usageCount increment (analytics groundwork for 4b).
- NOTE: preset musicMoods (library moods) NOT mapped to dashboard MOODS (manifestation theme) — left as-is; lockedTrackId is the concrete music carry-over. ctaText/visualKeywords/aspectRatio/watermark* stored but NOT yet consumed by pipeline (watermark render = P4c).

## Phase 3 details (DONE)
### Music — REAL LIBRARY (Batch 1 LIVE, $0/reel)
- **45 real Suno tracks** copied to `public/music-library/{abundant,calm,empowered,grateful,hype,inspired,joyful,stingers}/` (~55MB; public/** IS deployed via rsync). Counts: abundant 6, calm 5, empowered 7, grateful 5, hype 5, inspired 5, joyful 7, stingers 5. Batches 2&3 (~100 more) pending — just add files + extend manifest.json, no code change.
- `public/music-library/manifest.json` schema: `{version, track_count, tracks:[{id,title,file:"/music-library/{mood}/x.mp3",mood[],style[],duration_sec,energy,platforms:["reels","tiktok","shorts"],has_vocals:false,bpm:null,is_stinger}]}`. NOTE: field is `duration_sec` not `duration`; energy is 5-level `low|mid|mid-high|high|very-high`; bpm all null.
- **Library moods ≠ dashboard moods.** `lib/music-library.ts` has `MOOD_ALIASES` bridging dashboard moods (manifestation→[inspired,abundant], meditation→[calm], wealth-frequency→[abundant], cinematic→[empowered,inspired], dreamy→[calm,grateful], uplifting→[joyful,inspired], powerful→[empowered,hype], serene→[calm]) + synonyms → ordered LibraryMood[]. Do NOT change dashboard MOODS list (drives visuals/script). `MOOD_ENERGY` per library mood. `PLATFORM_ALIASES` (instagram-reels→reels, youtube-shorts→shorts).
- `lib/music-library.ts` (CLIENT-SAFE) exports: getAllTracks, getTrackById, scoreTrack, rankTracks, matchTrack, getAlternates, **getStingers, getStingerById, defaultStinger(platform)**, **moodCoverage()** (→{moods:[{mood,count,low:count<3}],stingers,total}). scoreTrack: mood primary +1000/fallback, style +200, platform +60, energy tiebreak. rankTracks hard-filters `has_vocals===false && !is_stinger`.
- Audio mix in compositor.ts: music -18dB under voiceover (sidechaincompress), -8dB in gaps; 300ms fade-in, 800ms fade-out; `-stream_loop 4` loops short tracks; output trimmed to video duration.
- **STINGERS (5 tracks) = optional intro/outro accent layer, default OFF.** Toggle in dashboard below MusicPicker (state `enableStinger`). Sends `stinger:bool` in handleGenerate → generate route → PipelineOptions.stinger/stingerId → pipeline resolves getStingerById||defaultStinger(platform), ensurePublicLocalAsset, passes `stingerUrl` to compositeReel. Compositor: input `in_stinger` (no loop), audio branches output `[amain]`, stinger split into intro(t=0)+outro(adelay T-3s) mixed at vol 0.7 → `[aout]`. scenesJson writes `stinger_track_id`.
- Custom music uploads: Pro=1 slot, Premium=5 (customMusicSlots in model-tiers.ts). `CustomMusic` prisma model. APIs: /api/music (GET list+slots / POST save), /api/music/match, /api/music/presign, /api/music/[id] DELETE.
- components/music-picker.tsx — mood-based icons+accent colors (MOOD_META: abundant=Gem/gold, calm=Waves/sky, empowered=Flame/purple, grateful=Heart/rose, hype=Rocket/orange, inspired=Sparkles/amber, joyful=Sun/yellow). Row shows icon tile + title + "duration · mood" (no BPM). Smart-match auto card, "Change track"→3 alternates, custom upload/delete UI. Free tier → settings link.
- **Coverage widget in /admin/margins**: route adds `musicCoverage: moodCoverage()`; page renders per-mood count chips (amber flag if <3) + stinger count + total. ffprobe cmd used to fill durations: `ffprobe -v error -show_entries format=duration -of csv=p=0 FILE`.

### Model tiers (lib/model-tiers.ts, CLIENT-SAFE)
- standard: Kling 2.5 Turbo + Flux 1.1 Pro = 5 coins/50 credits
- pro: Kling 2.5 Turbo Pro + Flux 1.1 Pro Ultra = 12 coins/120 credits
- cinematic: Veo 3 Fast + Flux 1.1 Pro Ultra (+Luma Ray2 b-roll planned) = 25 coins/250 credits
- modelTierAccess(subTier): free→[], pro→[standard,pro], premium→all. resolveModelTier clamps requested→allowed.
- **IMPORTANT: pro users CAN now do motion (standard/pro tiers) — old premium-only motion gate was relaxed in quota.ts checkGeneration(userId, motion, modelTierId).** Motion cost = getModelTier(effId).coinCost. Static still flat 1 coin.
- components/model-tier-picker.tsx — 3 cards w/ sample video preview (reuses /showcase/*.mp4), features, coins+credits, locked state.
- Providers: lib/providers/flux-image.ts (FluxImageProvider, fal queue→S3 re-upload), falai-video.ts rewritten model-aware (Veo3 vs Kling branches), index.ts param-aware getVideoProvider/getFluxImageProvider.

### Cost dashboard
- /api/admin/margins surfaces per-reel model_tier, music_track_id, voice_tier (parsed from reel.scenesJson). Displayed in app/admin/margins/page.tsx expanded row.
- Pipeline writes model_tier/music_track_id/voice_tier into scenesJson metadata. music_cost=0 logged.

## Dashboard wiring (app/dashboard/page.tsx)
- State: modelTier (default 'standard'), musicTrackId (null). <MusicPicker> always visible; <ModelTierPicker> shown when enableMotion && motionUnlocked (allowedTiers.length>0).
- handleGenerate body sends modelTier (when motion) + musicTrackId. Create button label uses getModelTier(modelTier).name/coinCost when motion.

## Known false positives (ignore in tests)
Duplicate-image warnings: testimonial avatars (/testimonials/*.jpg) + shared /voices/female.jpg avatar.
