

## Playable Reel Preview + Massively Expanded Voice Library (latest session)

### Reel playback (FIXED blank-frame bug)
- Root cause: generation pipeline was a MOCK that saved non-existent `/mock/*.mp4|mp3` paths, and the reel preview page had only a static placeholder div (no media element) → blank frame.
- `lib/reel-assets.ts` = single source of truth mapping reel.style→background video, reel.mood→music, reel.voice→sample. Exports `resolveReelAssets`, `getBackgroundVideo/Poster/MusicTrack/getVoiceSample`, `baseVoiceId` (strips `@speed` suffix), `isPlaceholderUrl`.
- Background videos MUST be the CLEAN `/showcase/{dream,selflove,wealth}.mp4` (no burned-in text). `/examples/*.mp4` have burned-in captions + watermark — DO NOT use as player bg (conflicts with live caption overlay). Posters at `/showcase/*-poster.jpg` (ffmpeg-extracted frames). Map: wealth/money/luxury→wealth, motivational/power/confidence→selflove, everything else (spiritual/abundance/manifestation/LoA/meditation/peace/love/gratitude)→dream.
- Music: 8 solfeggio-frequency tracks in `/public/reel-music/*.mp3` (manifestation, meditation, wealth-frequency, cinematic, dreamy, uplifting, powerful, serene), generated via ffmpeg sine+tremolo+fades. ALWAYS use `ffmpeg -nostdin` in loops (ffmpeg eats stdin → alternating commands silently fail).
- `lib/generation-pipeline.ts` now writes real `resolveReelAssets` URLs on completion. GET `/api/reels/[id]/route.ts` BACKFILLS placeholder/null urls via the helper (so old/seeded reels play) + adds `posterUrl`,`voiceSampleUrl`.
- `components/reel-player.tsx` (client): aspect-[9/16], `<video>` bg (loop/muted/playsInline) + `<audio>` music (vol 0.4) as MASTER CLOCK, gold play/pause overlay (animate-pulse-gold), live caption overlay synced to `scriptJson.fullScript` line start/endTimes (loops over ~28s), watermark badge if free tier, mute toggle. Wired into reel preview page; Download = `<a>` download of the video file.

### Massively expanded voices (dashboard "Pick a Voice")
- `app/dashboard/page.tsx` `VOICE_LIBRARY` now 8 categories: Female (15), Male (15), Mysterious (5), Historical (4), Biblical (4), Motivational (5), Educated (4), Meditation (5) = 57 preset voices. Style achieved via edge-tts `--rate`/`--pitch` per voice (script `/home/ubuntu/gen_voices.sh`).
- Samples in `public/voices/library/*.mp3` (57 files). New category covers `public/voices/{mysterious,historical,biblical,educated}.jpg` (AI-generated).
- Speed control: `speed` state (slow/normal/faster) segmented control under voice cards. Generate payload sends `voice: speed==='normal' ? id : `${id}@${speed}``. Pipeline/helper strip `@speed` via `baseVoiceId`.

## Voice Library + Custom Voice + Landing Hover Videos (earlier session)

### Landing hero hover-play videos
- `app/_components/landing-page.tsx` SHOWCASE entries each have a `video` field: `/showcase/wealth.mp4`, `/showcase/selflove.mp4`, `/showcase/dream.mp4` (clean, no-text, muted, looping 540x960 Ken Burns clips built via ffmpeg from CDN backgrounds).
- `ShowcaseReel` component plays video on mouseenter/touchstart, pauses+resets on mouseleave. "Hover to play" badge hides on hover.

### Expanded voice library (dashboard "Pick a Voice")
- `app/dashboard/page.tsx`: `VOICE_LIBRARY` = 4 categories (Female 6, Male 6, Meditation 3, Motivational 3) of preset voices. Derived `ALL_PRESET_VOICES` + `VOICE_CATEGORIES`.
- 18 real samples generated via edge-tts in `public/voices/library/*.mp3`. Category cover images at `public/voices/{female,male,meditation,motivational}.jpg` (reused as card bg for all variations in a category — intentional, not a real duplicate).
- UI: category tab row + "My Voices" tab. Active category shows horizontal variation cards (play/pause preview, gold border when selected). All `<audio>` rendered once in hidden block keyed by voice id so playback survives tab switches.
- Generate payload now sends `voice` as the voice id string (e.g. `female-aria`, or custom voice id). Pipeline treats voice as free-string label — no validation needed.

### Add Your Own Voice (record/upload)
- Cloud storage initialized (S3). `lib/aws-config.ts` + `lib/s3.ts` (presigned upload w/ ContentDisposition=attachment for public files — client MUST send `Content-Disposition: attachment` header on PUT).
- Prisma `CustomVoice` model (userId, name, cloudStoragePath, isPublic, durationSec, source, @@map custom_voices) + `customVoices` relation on User.
- API: `app/api/voices/route.ts` (GET list w/ playable URLs via getFileUrl, POST save record), `app/api/voices/presign/route.ts` (POST presigned URL), `app/api/voices/[id]/route.ts` (DELETE w/ ownership check + S3 delete).
- `components/add-voice-modal.tsx`: record (MediaRecorder, 30s max, audio/webm) OR upload (audio/*, 25MB max) with preview. onAdded returns `{id,name,source,audio}`.
- "My Voices" tab shows custom voice cards (play + delete) + dashed "Add Your Own Voice" card opening the modal.

### Asset generation notes (VM dev only, NOT in prod)
- ffmpeg + python3 available in VM dev env only — pre-generate all audio/video, commit to /public/. `bc` NOT available (use awk). edge-tts installs via pip.
- asset_retrieval_subtask saves to CDN, not local — must curl down manually.
