

## Subtitle font rendering (root cause + fix) — VERIFIED with real renders
- **Bug:** chosen subtitle font (e.g. Bebas Neue) did NOT render in final MP4 — libass fell back to default sans. Color/stroke/shadow worked (ASS override tags) but font family didn't.
- **Root cause (proven):** remote FFmpeg workers don't have Google fonts installed, AND the FFmpeg API BLOCKS the `fontsdir` option (any command with `fontsdir` → "Bad Request"). So `subtitles` filter couldn't locate the custom font.
- **Fix (in `lib/captions/font-embed.ts` + `lib/compositor.ts`):** embed the chosen `.ttf` as an MKV attachment; libass auto-loads embedded fonts. Pipeline: Step A = mux `.ass` + `.ttf` into tiny `.mkv` (stream copy) via FFmpeg API; **re-host that MKV to S3 via uploadPublicBuffer** (CRITICAL: cdn.abacus.ai FFmpeg-output URLs are UNREACHABLE as inputs to subsequent FFmpeg jobs — only S3 / fal.media URLs are fetchable as inputs); Step B = composite with `subtitles={{in_subs}}`. Graceful fallback to `{{in_ass}}` if embed fails.
- 15 bundled font files live in `public/fonts/subtitles/*.ttf` (family names match SUBTITLE_FONTS). `fontFileFor(family)` maps family→file.
- **WYSIWYG preview:** `app/globals.css` has 15 `@font-face` rules pointing to the SAME `/fonts/subtitles/*.ttf` files, so editor + reel-player preview use the exact fonts the server embeds. `components/reel-player.tsx` overlay now honors `subtitleStyle` (fontFamily/color/stroke/shadow/position/highlight) — pass `subtitleStyle={reel?.subtitleStyle}` from reel page.
- Variable fonts render bold correctly with ASS `Bold=-1` (verified, not Thin).

## Free-Tier Model — "1 REAL-AI Reel, Lifetime/Account" (server-enforced)
- **STRATEGY (current/locked):** Free = ONE real-AI reel per account, FOREVER (no daily renewal). Real generation: LLM script + AI images + auto-matched music + subtitle compositing. 5s / 720p / forced watermark / Standard model. ~$0.25/reel. Goal: one "wow" reel → upgrade decision → hard wall.
- **DB:** `User.freeReelUsed Boolean @default(false) @map("free_reel_used")`. Set true on success; reset to false in pipeline fatal-error catch (so failed reels don't burn the entitlement). All legacy users default false (everyone gets a fresh real-AI free reel — intentional/fair).
- `lib/free-tier.ts`: `FREE_LIFETIME_REELS=1`, `FREE_REELS_PER_IP_PER_HOUR=1`, `FREE_DAILY_BUDGET_CENTS=2000`, `FREE_REEL_EST_COST_CENTS=25`. validateFreeTierRequest/clampFreeTierParams unchanged (5s, 720x1280, standard tier, female-aria, karaoke, FREE_FONTS=['DM Sans','Bebas Neue','Anton'], FREE_COLORS=['#FFFFFF','#D4AF37','#7B2FBE'], no music picker/upload/stinger/brand presets/motion).
- `lib/free-tier-limits.ts`: checkFreeTierLimits now ONLY (1) per-IP 1/hour + (2) $20/day budget. `FreeBlockReason='ip_quota'|'budget_exhausted'` (daily_quota REMOVED). recordFreeTierSpend/getFreeBudgetToday use DailyFreeStat.
- `app/api/reels/generate/route.ts` free block: 403 email_unverified → 403 `free_lifetime_exhausted` (if freeReelUsed) → 403 free_locked (premium opts) → 429 ip_quota/budget_exhausted → **atomic claim** `updateMany({where:{id,freeReelUsed:false},data:{freeReelUsed:true}})` (count===0 → 403 race-safe) → freeClamp + coinCost=0. **Pipeline mode is now always 'full'** (real AI gen); watermark+720p still forced by tier==='free' (pipeline `isFree = isPreview || tier==='free'`). recordFreeTierSpend(25) after reel.create.
- `app/api/payments/subscription/route.ts` returns `quota.freeReelUsed` + `quota.emailVerified`.
- **Dashboard UX (`app/dashboard/page.tsx`):** `freeReelExhausted = isFreeTier && (freeReelUsed || quota?.freeReelUsed)`. handleGenerate sends CLAMPED body for free (voice female-aria, length 5, motion false, clamped subtitle). Persistent "You've used your free AI reel" banner → setShowPaywall. Create button: exhausted → "🔒 Upgrade to Create More" opens paywall. Locked controls w/ Pro badges: Reel Length (free shows single 5s + "15–30s with Pro" lock), Voice Settings (free shows fixed Aria card + "57 voices with Pro" lock), Cinematic Motion (Upgrade→), MusicPicker (tier-locked), SubtitleEditor lockedFree. limit modal + email-verify banner. **All upgrade CTAs route to /dashboard/settings (NO /pricing route exists).**
- **reel-player.tsx:** props `isFree`, `onUpgradeClick`. Tappable watermark (Crown badge) → onUpgradeClick. Post-playback upgrade wall (fires once on first play-through end, via onEnded for composited non-loop + timeupdate fallback): Free-vs-Pro side-by-side + "Upgrade to Pro →". Wired in reel detail page: `isFree={reel?.tier==='free'} onUpgradeClick={()=>router.push('/dashboard/settings')}`.
- Email verify: lib/email-verify.ts, /api/signup issues token, /api/auth/verify + /api/auth/resend-verification, /verify page. NOTIF_ID_EMAIL_VERIFICATION=b8a0c4884.
- Music: existing 45 tracks tagged source='curated_v1'. musicTrackId/musicSource persisted; /admin/margins shows musicSourceBreakdown + "Free Tier Budget — Today" widget.
- **Budget ceiling:** $20/day ÷ $0.25 = ~80 NEW free reels/day. Acceptable for launch; raise FREE_DAILY_BUDGET_CENTS if signups exceed.
