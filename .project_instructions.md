# ManifestReel AI — Project Memory

## Overview
AI-powered vertical (9:16) manifestation reel generator. Next.js 14 app at `/home/ubuntu/manifest_reel_ai/nextjs_space`.
Deployed at TWO untagged hosts — deploy each SEPARATELY (no deployment_tag, 2 deploy calls per release):
`manifest-reel-ai-qcjkqa.abacusai.app` and `manifestreelai.com`.

## Test accounts
- john@doe.com / johndoe123 (admin)
- demo@manifestreel.ai / demo123456 (pro)
- freetest@manifestreel.ai / freetest123 (free)

## Free tier
- Free reels are **7 seconds** (was 5s). Constant `FREE_DURATION_SEC = 7` in `lib/free-tier.ts`. `FREE_REEL_EST_COST_CENTS = 26`. coinCost=0 for free reels.
- IP quota: 1 free reel per IP per hour (`lib/free-tier-limits.ts` counts non-failed reels in last hour). Bypass for testing: set a recent reel's clientIp=null.

## Reel duration pipeline (CRITICAL root-cause knowledge)
- `scriptPlanForDuration` in `lib/providers/llm-script.ts` previously had a hard floor of `Math.max(10, ...)` → 16 words / 4 scenes min → ~8s narration even for short reels. THAT was the bug that forced the old 5s reels to render ~8s and made the duration check unreliable.
- FIX: short-reel branch — `if (target <= 9) { wordBudget = max(9, round((target-2.0)/0.55)); return { affirmationLines: 2, totalScenes: 3, wordBudget }; }`. Narration lands UNDER target; pipeline then EXTENDS the final scene to exactly the target → delta=0.
- `lib/generation-pipeline.ts` duration check (~line 625): ±1s gate is ENABLED for ALL tiers now (removed the old free-tier skip). Verified delta=0 for 7s free reels.

## Thumbnails (real first-frame, not generic template)
- After final MP4 render, `extractPosterFrame(videoUrl)` in `lib/compositor.ts` extracts a representative frame via the FFmpeg API and stores the CDN URL in `Reel.thumbnailUrl`.
- WORKING FFmpeg command: `-i {{in_1}} -vf "thumbnail=100,scale=540:-2" -frames:v 1 -q:v 3 {{out_1}}`. IMPORTANT: `-ss` seeking (input OR output) FAILS on the Abacus FFmpeg API ("Output file out_1 not found") — use the `thumbnail` filter instead, double-quoted in the command string.
- API precedence: `app/api/reels/[id]/route.ts` and `app/api/reels/route.ts` prefer the real `thumbnailUrl` over the generic style poster via `isPlaceholderUrl()`.
- Dashboard "My Reels" cards + library cards render `reel.thumbnailUrl` as an `aspect-[9/16]` `<img>` with a Film-icon fallback.
- Composited videoUrl is a permanent `cdn.abacus.ai/videos/...mp4`; extracted thumbnails are `cdn.abacus.ai/images/...jpg`.

## DB script pattern
`cd nextjs_space && set -a && source .env && set +a && node -e "..."` using `@prisma/client`. passwordHash is a User field. For /tmp scripts add `export NODE_PATH="$(pwd)/node_modules"`.

## Conventions
- Both deploys often say "No changes detected since last checkpoint" — normal, deploy still succeeds.
- Pre-existing duplicate-image test warnings (testimonials, voices/female.jpg, showcase posters) are known/ignored.
- Next phase: Phase 5 = Pricing & Credits (all free-tier copy now says "7s").
