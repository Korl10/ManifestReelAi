## Reel Generation Pipeline (rebuilt for premium/viral quality)

The generation pipeline (`lib/generation-pipeline.ts`) now produces REAL composited vertical MP4s (1080x1920, h264+aac) via the Abacus FFmpeg API, replacing the old mock/stock-loop flow. Flow: script -> images -> voice -> music -> composite.

### Provider architecture (`lib/providers/`)
- `index.ts` exports `getScriptProvider` (LLMScriptProvider, model env SCRIPT_MODEL default gpt-5.4), `getImageProvider` (AbacusImageProvider, model env IMAGE_MODEL default gemini-3.1-pro, 9:16), `getVoiceProvider` (ElevenLabsVoiceProvider), `getMusicProvider` (LibraryMusicProvider).
- `fallback.ts`: `buildTemplateScript()` (curated affirmation templates w/ per-scene cinematic image prompts + timings) and `fallbackSceneImages()` (rehosts bundled /public/styles/*.jpg to public S3, rotated across scenes).
- Every step is wrapped in try/catch with graceful fallback so reels are ALWAYS produced.

### Phasing
- Phase 1 (DONE, no keys): LLM scripts + AI images when credits available; else template script + bundled stills. Library music. Karaoke captions. Real composite.
- Phase 2 (DONE): ElevenLabs premium voice with forced alignment. Key: ELEVENLABS_API_KEY configured. Voice IDs: EL_VOICE_FEMALE (Rachel 21m00Tcm4TlvDq8ikWAM), EL_VOICE_MALE (Adam pNInz6obpgDQGcFmaJgB), EL_VOICE_SOFT (Bella EXAVITQu4vr4xnSDxMaL). Model: EL_MODEL (eleven_multilingual_v2). Speed: 0.92 default (cinematic), @slower=0.8, @faster=1.12. voice_cost ~$0.10/reel. Total per-reel cost with voice: ~$0.43.
- Phase 3 (BUILT + LIVE-TESTED): fal.ai image-to-video (Kling 2.5 Turbo Pro = `fal-ai/kling-video/v2.5-turbo/pro/image-to-video`) for HYBRID motion. FAL_KEY in .env (format key_id:key_secret, auth header `Key <FAL_KEY>`).
  - Provider `lib/providers/falai-video.ts` (FalaiVideoProvider). Uses fal QUEUE API via raw fetch: POST `https://queue.fal.run/<model>` → poll status_url (202 IN_PROGRESS / 200 COMPLETED) → GET response_url → `video.url`. Params: image_url (public S3), prompt (MOTION-ONLY, see below), duration '5', cfg_scale 0.35 (subtle motion), negative_prompt. MAX_CONCURRENCY=2, CLIP_TIMEOUT 240s. getVideoProvider() in index.ts.
  - CRITICAL content-policy lesson: fal runs a content checker that 422-rejects prompts (content_policy_violation). The manifestation niche's image descriptions ("divine", "sacred temple", "sacred geometry") TRIP IT. FIX: for image-to-video send ONLY the motion-direction template (camera + atmosphere), NOT the scene description (the source image already defines content). `motionPromptFor()` in falai-video.ts does this via resolveMotionThemeFromPrompt. Also the bundled stock still spiritual.jpg itself gets flagged (a person/deity image); AI-generated scene images are usually fine. 422 → bail immediately (permanent), clip→null→Ken Burns fallback.
  - Motion prompt templates in `lib/providers/motion-prompts.ts`. Four themes: cosmic, wealth, water, nature — cinematic camera+atmosphere strings (NO flaggable subject words). `resolveMotionTheme(style)` maps style→theme. `resolveMotionThemeFromPrompt(imagePrompt, style)` keyword-detects theme from the IMAGE PROMPT text (preferred, per user). `selectHeroScenes(totalScenes, max=4)` → hook(0)+~35%+~60%+last.
  - Pipeline STEP 2.5 (`generation-pipeline.ts`): if !preview && reel.motion && FAL_KEY → selectHeroScenes(sceneCount,4), generate clips, pass `sceneClipUrls` to compositor, log `costBreakdown.video_cost`. scenesJson stores motionProvider, motionClipCount, per-scene clipUrl. Fails gracefully (any/all clips → Ken Burns).
  - Compositor (`lib/compositor.ts`) accepts optional `sceneClipUrls: (string|null)[]`. Motion scene: input `-i {{in_i}}` (video), filter scale/crop to 1080x1920 + fps=30 + tpad clone-pad + trim to exact scene duration. Still scene: existing zoompan Ken Burns. xfade chain/captions/audio unchanged. VALIDATED: golden-liquid wealth clips animated + composited correctly.
  - LIVE COST TEST (4 motion clips, wealth style, 7 scenes): video_cost = $1.40 (4×$0.35 Kling 2.5 @ $0.07/s × 5s). Full real motion reel cost ≈ script $0.02 + image $0.28 + voice $0.11 + music $0 + video $1.40 + render $0.02 + storage $0.01 = ~$1.84/reel (this test showed $1.56 because image gen had a transient 400 and fell back to free stills).
  - COIN PRICING DECISION (pending user approval): at cheapest per-coin price (Studio $74.99/150 = $0.50/coin): 4 coins=$2.00 → only $0.16 margin (thin); 5 coins=$2.50 → $0.66 margin (healthy). Recommend EITHER 5 coins @ 4 clips OR keep 4 coins but cap at 3 clips ($1.05 video → ~$1.49 total → $0.51 margin). Current code: COIN_COST.motion=4, selectHeroScenes caps 4 → must change one. AWAIT user choice before editing pricing/scaling.
  - image-gen.ts now retries each scene image up to 3× (transient 400/429/5xx) so one hiccup no longer forces whole reel to stills.
- Phase 4: music library now, Suno later.

### Coin / pricing model (IMPLEMENTED)
- Tiers: FREE (no paid APIs — demo gallery + ONE watermarked preview from cached/sample assets, lifetime cap 1), PRO $19.99/mo → 30 coins (static only), PREMIUM $49.99/mo → 60 coins (motion enabled).
- COIN_COST: static reel = 1 coin, motion reel = 4 coins (`lib/pricing.ts`).
- COIN_BUNDLES (one-time, Stripe, stack on subscription, valid 12 months): starter 10/$7.99, creator 25/$16.99 (popular), pro-pack 60/$34.99, studio 150/$74.99.
- Rules: coins gate VOLUME within a tier; features gated by TIER. Motion stays Premium-only even if a Pro user buys coins. Subscription coins reset monthly (UsageCounter window); bundle coins valid 12 months.
- `lib/quota.ts`: `getCoinBalance(userId)`, `checkGeneration(userId, motion)` (enforces inactive/free/motion_locked/insufficient_coins), `consumeCoins(userId, amount)` (subscription monthly first, then bundle FIFO non-expired). `checkQuota` kept as legacy shim returning reelsUsed=subscriptionUsed, reelsCap=subscriptionCoins+bundleCoins, bonusReels=bundleCoins, plus coinsAvailable/subscriptionRemaining/bundleCoins/motionEnabled.
- Schema: Reel.motion (Bool), Reel.coinCost (Int default 1). CoinPurchase.coinsRemaining (Int) + expiresAt (DateTime?). Webhook sets coinsRemaining + 12mo expiry on coin_purchase completion.
- generate route: accepts `motion` flag; free → preview pipeline mode (no paid APIs), no coin consume; paid → checkGeneration + consumeCoins up-front, sets reel.motion+coinCost.
- Pipeline `runGenerationPipeline(jobId, reelId, userId, mode)` — mode 'preview' forces template script + fallback stills + no voice + library music, watermark always.
- TODO after Kling cost test: confirm whether motion should cost 4 or 5 coins to stay margin-positive at cheapest per-coin bundle (Studio = $74.99/150 = $0.50/coin → 4 coins = $2.00 revenue; report actual fal.ai cost vs this).

### LLM credits (RESOLVED)
Abacus LLM API text + image now working (credits replenished). Image model = gpt-5.4 (gemini-3.1-pro is REJECTED by API). Graceful fallbacks remain if credits ever lapse.

### Compositor (`lib/compositor.ts`) — VALIDATED end-to-end
- FFmpeg API: createRunFfmpegCommandRequest / getRunFfmpegCommandStatus, deployment_token=ABACUSAI_API_KEY.
- Per-scene Ken Burns (scale 2160x3840 then zoompan to 1080x1920, alternating zoom in/out), xfade crossfades (0.6s cycling transitions), burns ASS karaoke subtitles, mixes voice(1.0)+music(0.28) or music-only(0.6) or voice-only or -an.
- input_files keys in_1.., in_music, in_voice, in_ass. ASS passed as input, referenced via subtitles={{in_ass}}. filter_complex MUST be double-quoted. vcpu_count 16, max 600s.
- Output is a permanent cdn.abacus.ai/videos/*.mp4 URL.

### Captions (`lib/captions/ass.ts`) — VALIDATED
ASS karaoke: white (&H00FFFFFF), active word purple #7B2FBE (&H00BE2F7B) scaled 116%+bold, dim upcoming. PHRASE_SIZE=3 words. Font DejaVu Sans, alignment 2, MarginV 430. ASS colors are &HAABBGGRR. Optional watermark event.

### Media storage (`lib/media-storage.ts`)
uploadPublicBuffer / uploadPublicText / ensurePublicLocalAsset (content-hash cached upload of /public assets to public S3 so FFmpeg workers can fetch). Public files under ${folderPrefix}public/...

### Schema additions (Reel model)
scenesJson Json? (stores {composited, scriptProvider, imageProvider, voiceProvider, scenes:[{text,imageUrl,durationSec}]}) and durationSec Float?. Nullable = compatible, no data loss.

### Player (`components/reel-player.tsx`)
New `composited` prop. When true (videoUrl is cdn.abacus.ai composite): play video WITH baked-in audio (mute toggle controls video), NO overlay captions/music/watermark (all burned in). When false (legacy /showcase stock): muted video loop + separate music element + live caption overlay + watermark overlay. Reel page derives isComposited from scenesJson.composited.

### Assets
- /public/styles/*.jpg: spiritual, law-of-attraction, abundance, meditation, wealth, luxury, motivational (fallback scene stills).
- /public/reel-music/*.mp3: 8 solfeggio/ambient tracks (mood-mapped in reel-assets.ts getMusicTrack).
- /public/voices/library/*.mp3: 57 voice samples.
- Color theme: gold #D4AF37 + royal purple #7B2FBE.
