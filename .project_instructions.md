# ManifestReel AI — Project Memory

## Overview
AI-powered vertical (9:16) manifestation reel generator. Next.js 14 app at `/home/ubuntu/manifest_reel_ai/nextjs_space`.
Deployed at TWO untagged hosts — deploy each SEPARATELY (no deployment_tag, 2 deploy calls per release):
`manifest-reel-ai-qcjkqa.abacusai.app` and `manifestreelai.com`.

## Test accounts
- john@doe.com / johndoe123 (admin)
- demo@manifestreel.ai / demo123456 (pro)
- freetest@manifestreel.ai / freetest123 (free)

## Free tier
- Free reels are **7 seconds** (was 5s). Constant `FREE_DURATION_SEC = 7` in `lib/free-tier.ts`. `FREE_REEL_EST_COST_CENTS = 26`. coinCost=0 for free reels.
- IP quota: 1 free reel per IP per hour (`lib/free-tier-limits.ts` counts non-failed reels in last hour). Bypass for testing: set a recent reel's clientIp=null.

## Reel duration pipeline (CRITICAL root-cause knowledge)
- `scriptPlanForDuration` in `lib/providers/llm-script.ts` previously had a hard floor of `Math.max(10, ...)` → 16 words / 4 scenes min → ~8s narration even for short reels. THAT was the bug that forced the old 5s reels to render ~8s and made the duration check unreliable.
- FIX: short-reel branch — `if (target <= 9) { wordBudget = max(9, round((target-2.0)/0.55)); return { affirmationLines: 2, totalScenes: 3, wordBudget }; }`. Narration lands UNDER target; pipeline then EXTENDS the final scene to exactly the target → delta=0.
- `lib/generation-pipeline.ts` duration check (~line 625): ±1s gate is ENABLED for ALL tiers now (removed the old free-tier skip). Verified delta=0 for 7s free reels.

## Thumbnails (real first-frame, not generic template)
- After final MP4 render, `extractPosterFrame(videoUrl)` in `lib/compositor.ts` extracts a representative frame via the FFmpeg API and stores the CDN URL in `Reel.thumbnailUrl`.
- WORKING FFmpeg command: `-i {{in_1}} -vf "thumbnail=100,scale=540:-2" -frames:v 1 -q:v 3 {{out_1}}`. IMPORTANT: `-ss` seeking (input OR output) FAILS on the Abacus FFmpeg API ("Output file out_1 not found") — use the `thumbnail` filter instead, double-quoted in the command string.
- API precedence: `app/api/reels/[id]/route.ts` and `app/api/reels/route.ts` prefer the real `thumbnailUrl` over the generic style poster via `isPlaceholderUrl()`.
- Dashboard "My Reels" cards + library cards render `reel.thumbnailUrl` as an `aspect-[9/16]` `<img>` with a Film-icon fallback.
- Composited videoUrl is a permanent `cdn.abacus.ai/videos/...mp4`; extracted thumbnails are `cdn.abacus.ai/images/...jpg`.

## DB script pattern
`cd nextjs_space && set -a && source .env && set +a && node -e "..."` using `@prisma/client`. passwordHash is a User field. For /tmp scripts add `export NODE_PATH="$(pwd)/node_modules"`.

## Voice library (160 curated professional voices)
- `lib/voice-catalog.ts` = single source of truth. **160 voices**, 160 UNIQUE ElevenLabs IDs (fixed prior bug where catalog reused only 22 premade IDs — e.g. Aria/Aria-Soft both pointed to Rachel `21m00Tcm4TlvDq8ikWAM`).
- Counts per category: Mysterious 20, Historical 20, Biblical 20, Motivational 20, Educated 20, Meditation 20, **Male 20, Female 20**. Gender splits curated per category.
- All voices are ElevenLabs **shared/professional** tier (NOT premade defaults). Curated from the shared library (1764 unique professional EN voices) scored on accent diversity, age diversity, multilingual, popularity. Alternates NOT cached (cleared on VM restart); re-curate on demand for swap requests.
- `VoiceEntry` fields: id (internal e.g. `mys-m-01`), name, elevenLabsId, gender, ageRange (young/middle/senior), accent, language, category, useCases[], description, defaultTier, supportedTiers[], `multilingual: bool`, `previewUrl` (ElevenLabs-hosted mp3 for instant zero-cost card playback), `samplePath` (legacy/empty).
- New exports: `CATEGORY_DESCRIPTIONS`, `CATEGORY_TEST_PHRASES` (per-category audition phrase), `SPEED_PRESETS={slow:0.75,normal:1.0,fast:1.2}` (WIDENED from 0.85/1.0/1.15 so the slider is audibly distinct), `SpeedPreset`, `speedValue(preset)` helper (parses `@slow`/`@fast` suffix or numeric; clamp 0.7–1.2).
- KEY FACT: ElevenLabs TTS works with ANY shared/professional voice_id WITHOUT adding to the account library (HTTP 200). No voice-slot limit → 150 voices feasible. The API key is permission-scoped: `/v1/user/subscription` fails (no user_read) but TTS + shared-voices + listing all work.

## Voice speed (native param, NOT FFmpeg)
- Speed presets are ElevenLabs' NATIVE `voice_settings.speed` (0.75 slow / 1.0 / 1.2 fast). Proven server-side: distinct durations slow 4.37s / normal 3.76s / fast 2.79s for same text. NO atempo/FFmpeg.
- **SPEED-SLIDER BUG FIX (this turn)**: server side was always correct. The bug was 100% CLIENT-side in `components/voice-browser.tsx` — it (a) never sent `speed` in the preview POST body, and (b) cached `previewUrls` by `voice.id` alone, ignoring speed. FIX: cacheKey is now `${voice.id}__${voiceTier}__${speed}__${showAdvanced ? `${stability}_${similarity}` : 'def'}` and `speed` is sent in the POST body. `app/dashboard/page.tsx` now passes `speed={speed}` prop to `<VoiceBrowser>`.
- Dashboard sends `voice` as `${voiceId}@${speed}` (just `voiceId` when normal). `lib/providers/elevenlabs-voice.ts` `speedFromPreset()` → `speedValue()`. `lib/reel-assets.ts` strips `@speed`.
- Script word budget scales with speed: `scriptPlanForDuration(targetDuration, speed)` in llm-script.ts multiplies wordBudget by speed factor. `ScriptInput.voicePreset` carries `reel.voice`; pipeline passes it at generation-pipeline.ts ~line 232.

## Voice picker + Voice Lab
- Picker = `components/voice-browser.tsx` (dynamic import in dashboard). Category-grouped, search + gender/accent/useCase filters, per-voice preview (calls `/api/voices/preview` with first script line, falls back to `previewUrl`), tier selector, stability/similarity sliders. Shows gender symbol + ML badge for multilingual.
- Catalog API `app/api/voices/catalog/route.ts` returns voices + filters + `categoryDescriptions` + `testPhrases`. Auth-gated.
- Preview API `app/api/voices/preview/route.ts` accepts `{voiceId,text,tier,speed,stability,similarity}`, caches by md5(voiceId:tier:speed:text) 24h, returns `audioUrl`. Lazy/on-demand (no pre-rendered samples → cost-efficient).
- **Voice Lab** = admin-only sample sheet at `/admin/voice-lab` (tab in admin-shell). Category tabs; each voice row = metadata + 3 speed play buttons (slow/normal/fast, plays that category's test phrase) + Keep/Swap toggle. Decisions persist in localStorage; "Save my decisions" copies JSON (swapVoiceIds + byCategory) to clipboard. Built for the OWNER's ear-audit — I cannot audition audio subjectively.
- **Dashboard voice picker** (`app/dashboard/page.tsx`): rewired from hardcoded VOICE_LIBRARY → dynamic catalog-sourced array (imports `VOICE_CATALOG` + `CATALOG_CATEGORIES` from `lib/voice-catalog.ts`). Default voice = `female-f-01`. Audio playback uses lazy `new Audio()` on first play (no hidden <audio> elements). Voice count banner: "160 natural AI voices across styles".

## Music library infra
- **Vol. 1**: `lib/music-library.ts` + `public/music-library/manifest.json`: 40 tracks (28-190s, avg 38s) + 5 stingers. Weighted random matcher by mood/energy/category. Local files in `/public`, uploaded to S3 at generation time via `ensurePublicLocalAsset`.
- **Vol. 2**: `LibraryTrack` Prisma model. Uploaded via admin Music Loader → S3 cloud storage. FFmpeg processing normalizes EVERY track to exactly 60s: `-stream_loop -1 -i input -t 60` (loop-fills short tracks, trims long), + 0.5s fade-in + 0.8s fade-out at 59.2s + LUFS -14 + 192k MP3. All masters are uniform 60s. DB stores `cloudUrl` (processed) + `originalUrl` (raw). No local files needed. Minimum accepted Suno output: ≥35s.
- **Smart matcher** (`matchTrackAsync`): combines Vol. 1 manifest tracks + Vol. 2 DB tracks. Scoring: mood (via MOOD_ALIASES) → style overlap → platform fit → energy fit → provenance. `isCloudTrack()` helper tells pipeline whether to use `ensurePublicLocalAsset` (local) or direct URL (cloud).
- **Category mapping** (`lib/music-categories.ts`): 8 categories (spiritual, luxury, motivational, mysterious, historical, biblical, educated, meditation) → mood/style/energy defaults. `parseTrackFilename()` parses `{category}_{nn}_{descriptor}.mp3`.
- **Admin Music Loader** (`/admin/music-loader`): drag-drop MP3s, filename auto-parse, dry-run mode (preview tags without commit), batch upload+process. API at `/api/admin/music-loader` (GET: list, POST: upload+process) + `/api/admin/music-loader/presign` (presigned URLs).
- **Pipeline integration**: `generation-pipeline.ts` uses `matchTrackAsync` (async, includes DB tracks). V2 cloud tracks skip `ensurePublicLocalAsset` — their `cloudUrl` is already a public S3 URL usable by the FFmpeg compositor.
- Compositor uses `-stream_loop 4` for music input → loops track if shorter than reel. `amix=inputs=2:duration=longest` + fade-out at T-0.8s handles truncation naturally.

## Subtitle Editor (rebuild — Live Preview, Shadow, Presets, Fonts)
- File: `components/subtitle-editor.tsx`. Render engine `lib/captions/ass.ts` + types `lib/captions/subtitle-types.ts` (DO NOT touch ass.ts timing/burn-in — engine is genuinely complete).
- **Shadow control**: replaced forced Depth slider with a **Shadow [Off|On] toggle, default OFF**. When On reveals Opacity (0–100%, default 60%), Depth (0–8px, default 2px), Shadow color picker.
  - Added `shadowOpacity: number` field to `SubtitleStyle` + `DEFAULT_SUBTITLE_STYLE` (default 60). Flipped `DEFAULT_SUBTITLE_STYLE.shadowEnabled` true→**false**.
  - In ass.ts: when no highlight box, BackColour = `hexToAss(s.shadowColor, round((1-(shadowOpacity??60)/100)*255))` (BorderStyle=1 uses BackColour as shadow colour). `Shadow = shadowEnabled ? shadowDepth : 0` → **Shadow field is 0 in the .ass Style line when off** (verified via buildAss test: OFF→`...,1,0,0,2,...` ON→`...,1,4,4,2,...`). No `\shad` tag drawn when off.
- **CapCut-style presets**: horizontal row above the Font/Colors/Animation accordions, 5 one-click presets that atomically overwrite style fields: Clean White (Inter/karaoke), Bold Yellow (Anton/pop), Pill BG (DM Sans/highlight/fade-in), Karaoke Pop (Bebas Neue/karaoke/#FF3B30), Minimal (Inter/fade-in). **Preset row HIDDEN when `lockedFree`** (presets use Pro-only fonts/colors → protects free-tier limits).
  - DEVIATION: spec's "Minimal" wanted animation "none" but `SubtitleAnimation` type has NO 'none' value (adding it = render-engine change, out of scope) → uses 'fade-in' as minimal equivalent.
- **Fonts**: all 15 @font-face already in `app/globals.css` (font-display swap, DM Sans weight 400 700). Component now WAITS for font load before showing preview text: `useEffect` on `value.fontFamily` calls `document.fonts.load("400/700 40px '<fam>'")` → `document.fonts.ready`, gates preview with `visibility:hidden` until `fontReady`. Prevents FOUT/stacking (DM Sans now crisp).
- **Live Preview**: real animated word-by-word playback. Demo phrase from `previewText` prop (dashboard passes the user's prompt or "Your abundance is flowing toward you now"). ~350ms/word, ~850ms hold, loops via `cycle` state; ▶ Replay button. CSS keyframes `subFade/subPop/subSlideUp/subBounce/subType` honor all 6 animations; karaoke shows whole phrase + active-word highlight (activeWordColor + scale), others reveal word-by-word. Honors platform safe zone + position.
- Editor callers (props unchanged): `app/dashboard/page.tsx` L993 (lockedFree=isFreeTier), `app/dashboard/craft/_components/preset-wizard.tsx`.
- All 6 acceptance criteria browser-verified on localhost (pro + free accounts): shadow default off, 5 presets 1-click, DM Sans crisp, animated 7-word preview loops, 6 animations, free tier still 3 fonts/karaoke-only/no presets.
- **Garbled-text fix**: replaced `-webkit-text-stroke` (paints OVER fill, destroyed narrow fonts like Bebas Neue) with 8-directional `text-shadow` outline via `buildStrokeShadow(color, widthPx)` helper. Preview `textShadow` combines `buildStrokeShadow(strokeColor, strokeWidth/4)` + optional drop-shadow. Bebas Neue/Anton/DM Sans verified crisp.
- **No-subtitles toggle**: `subtitlesEnabled: boolean` added to `SubtitleStyle` (default `true`). Dashboard shows an On/Off toggle switch next to "Subtitle Style" header. When Off: editor + customize button hidden, "Subtitles are turned off" message shown, and the compositor (`lib/compositor.ts`) skips ASS build, font embed, and the `subtitles=` filter entirely — watermark overlay references `afterSubLabel` instead of `vsub`. Backward compatible: old presets/data without the field default to `true` (compositor checks `=== false`). Free tier can also toggle off.
- **Slider follow-up (filled track + Y-offset sync)**: shared `RangeInput` now renders a **gold filled track** via inline `linear-gradient` (`((value-min)/(max-min))*100%`) so current position is always visible (fixes "empty right side looks dead" confusion). Thumb `w-7 h-7`, `border-2 border-white`, `cursor-grab`/`active:cursor-grabbing`, `touch-none`. Right-to-left drag verified (Y Offset → -158px). Live-preview subtitle wrapper now applies `transform: translateY()` from `customYOffset` (scale 0.25 since preview ≈480px vs 1920px video; +value moves text DOWN for top anchor, UP for bottom/center) so Y Offset visibly moves preview in sync. `maxCharsPerLine`/`maxLines` persist to state (render-time split params; don't visibly change the single-phrase demo). Applies uniformly to ALL sliders (Size, Words Per Phrase, Y Offset, Max Chars/Line, Max Lines, Stroke, Shadow Opacity/Depth, Highlight Opacity).

## Conventions
- Both deploys often say "No changes detected since last checkpoint" — normal, deploy still succeeds.
- Pre-existing duplicate-image test warnings (testimonials, voices/female.jpg, showcase posters) are known/ignored.

## Pricing (Phase 5 — COMPLETE)
4 monthly subscription tiers + 5 top-up bundles. Duration-based coin costs per model tier.

### Subscriptions
| Tier | Monthly | Coins | Model Tiers | Auto-Post |
|------|---------|-------|-------------|-----------|
| Starter | $19.99 | 200 | Standard + Pro | Manual only |
| Pro | $39.99 | 500 | All 3 | IG + TikTok |
| Premium | $89.99 | 1,200 | All + 4K | IG/TikTok/YT/X |
| Agency | $199 | 3,000 | All + white-label | Multi-account |

Annual = **20% off** (was 50%, changed Phase 5b). Intro pricing for first 3 months.

**Founders' Pricing** (first 90 days from launch 2026-06-07) — CORRECTED Phase 5c:
Applies to **ANNUAL billing for ALL 4 tiers** (NOT Premium-only, NOT monthly). Deeper than standard 20% annual discount (33–38% off). **Locked for life** — founder keeps discounted annual rate on every renewal. Auto-reverts to standard 20% annual after Day 90.
| Tier | Founders Annual/mo | Annual Total | Discount |
|------|-----|-----|-----|
| Starter | $12.99 | $155.88/yr | 35% off |
| Pro | $24.99 | $299.88/yr | 38% off |
| Premium | $59.99 | $719.88/yr | 33% off |
| Agency | $129 | $1,548/yr | 35% off |
- Constants in `lib/pricing.ts`: `LAUNCH_DATE='2026-06-07'`, `FOUNDERS_DURATION_DAYS=90`, `FOUNDERS_ANNUAL_PRICE: Record<PlanTier,number>` (cents/yr), helpers `foundersAnnualPerMonth(tier)`, `foundersAnnualSavingsCents(tier)`, `isFoundersPeriod()`, `foundersCountdownDays()`.
- Checkout (`app/api/payments/create-checkout/route.ts`): when `isAnnual && isFoundersPeriod()`, overrides annual unitAmount with `FOUNDERS_ANNUAL_PRICE[tier]`; founders flag in metadata + description. Stripe price_data is created ON-DEMAND (no hardcoded Price IDs) → "locked for life" relies on Stripe keeping the subscription's original price_data on renewal.
- Display surfaces founders-aware annual pricing: landing-page (strikethrough monthly + founders/mo + Save badge + "locked for life" badge), settings page (label "(Founders)" + countdown subtext), paywall-modal (🔥 Founders label). All use client-side `useEffect`-gated flag to avoid hydration mismatch.

### Phase 5 verification answers (for owner)
1. **Trials**: TRIAL_CONFIG in pricing.ts — Starter 8c, Pro 40c, Premium 170c, **Agency 170c (trial EXISTS, same as Premium/cinematic)**. Card upfront, watermark, auto-convert. Intro offers (introMonthlyPrice) apply to MONTHLY billing only.
2. **Annual coin delivery = MONTHLY DRIP** (matches spec). `lib/quota.ts` UsageCounter per calendar month; coins refresh each month, NOT 12x upfront. No Month-1 burn risk.
3. **Coin rollover = NOT IMPLEMENTED** (divergence). Subscription coins RESET to zero each month (no carryover). Only purchased BUNDLE coins persist (12-mo expiry). Spec wanted 1-month rollover w/ 60-day expiry — future work.
4. **Auto-posting = PLACEHOLDER/DEMO** (not live). `/api/social/schedule` only sets DB fields + returns manual-post message. No OAuth/connectors. = Phase 6.
5. **Landing UI**: confirmed (annual default, annual LEFT/monthly RIGHT, Save badge, upsell link, Pro=⭐MOST POPULAR `xl:scale-[1.07]`, Premium per-day). Pro scale is 107% not exactly 110%.

**Landing page pricing UI**:
- Toggle: Annual ✓ (Save 20%) on LEFT, Monthly on RIGHT, default Annual
- Annual view: big annualMo price, "billed annually" subtext, green "💰 Save $X/year" badge
- Monthly view: price, "billed monthly" subtext, "Switch to annual" upsell link
- Pro card: ⭐ MOST POPULAR badge top-right, `xl:scale-[1.07]`, gold gradient border/glow
- Premium card: "= just $2.40/day — less than a coffee ☕" (annual), founders strikethrough (monthly)
- Agency tier added to landing page (3,000 coins, 5 seats, white-label, bulk gen)

### Per-Reel Coin Costs (model tier × duration)
| | 5s | 10s | 15s | 25s | 30s |
|------|---|---|---|---|---|
| Standard | 6 | 8 | 10 | 14 | 16 |
| Pro | 25 | 40 | 70 | 100 | 110 |
| Cinematic | 90 | 170 | 250 | 400 | 420 |

### Top-Up Bundles
Mini $9.99→100c, Creator $24.99→280c, Studio $49.99→600c, Pro Pack $99.99→1,300c, Agency Pack $249→3,500c

### Key files
- `lib/pricing.ts` — all plans, bundles, coin costs, trials, helpers
- `lib/model-tiers.ts` — model tier definitions, `modelTierAccess()`, `customMusicSlots()`
- `lib/quota.ts` — `checkGeneration(userId, motion, modelTier, targetDuration)` with duration-aware gating
- ALLOWED_LENGTHS in generate route: `[5, 10, 15, 25, 30]`
- Dashboard default duration: 15s
- Pipeline refund uses `reelCoinCost(tierId, duration)` for downgrade delta

## Coin Rollover (churn-prevention feature)
- **1-month rollover, 60-day expiry, capped at 1× current plan's monthly allotment.**
- `CoinRollover` Prisma model: `id, userId, coins (total rolled), remaining (decremented on use), fromMonth, expiresAt, createdAt`.
- Rollover created automatically when `getOrCreateCounter()` in `lib/quota.ts` creates a new month's counter — checks previous month's counter for unused coins, caps at `TIER_COINS[currentTier]`, 60-day expiry, idempotent (checks `fromMonth` uniqueness).
- Consumption order: subscription coins → rollover (oldest FIFO) → bundle coins.
- `CoinBalance` + `QuotaCheckResult` interfaces now include `rolloverCoins: number` + `rolloverInfo: RolloverInfo[]` (with `expiresAt` per record).
- Dashboard display: Settings page shows "🔄 X rolling over • expires [date]" line, main dashboard shows "• X rolled over" in coin bar.
- Pricing page note: "🔄 Unused coins roll over for 60 days (capped at 1× monthly allotment)."

## Agency Tier Changes
- **No trial** — `TRIAL_CONFIG` in `lib/pricing.ts` is now `Partial<Record<PlanTier, ...>>`, agency key removed.
- Agency card on landing page: "✅ 30-day money-back guarantee" badge + "or Talk to Sales →" mailto link below CTA.

## Auto-Posting Status
- Phase 6 DEFERRED. Owner submitting developer apps this week (IG/TikTok/YT/X), 1-6 week approval.
- Features labeled "(coming soon)" on Pro ("Auto-post IG + TikTok (coming soon)") and Premium ("Auto-post all platforms (coming soon)").
- Footer note: "Auto-posting launches in 30 days — Pro+ early access."
- Do NOT build OAuth flows until first API is approved.

## Pro Card Scale
- Exactly 110% (`xl:scale-[1.10]`) — was 107%, bumped per owner request.

## Phases & Status
- Phase 4d (Craft presets): FROZEN until voice audit complete + first full category drop verified live.
- **Phase 5 (Pricing overhaul): COMPLETE** — deployed to both hosts.
- **Phase 5c (Rollover + Agency + auto-post labels): COMPLETE** — deployed.
- Phase 6 (Auto-Posting): DEFERRED pending API approvals.
- **Voice Audit (COMPLETE — soft-delete phase)**: 9 voices marked `active: false` in `lib/voice-catalog.ts`. 151 active voices now served. Replacement voices expected within 7 days (2 Biblical, 1 Motivational, 2 Educated, 3 Meditation, 1 Female).
  - Retired: `bib-m-04` (Joseph), `bib-m-05` (David), `mot-m-11` (Shrey), `edu-m-09` (Wade), `edu-m-10` (Gilbert), `med-m-02` (Stephen), `med-m-03` (Darren), `med-m-05` (Ali), `female-f-19` (Anya)
  - Active category counts: Female 19, Male 20, Mysterious 20, Historical 20, Biblical 18, Motivational 19, Educated 18, Meditation 17
  - `ACTIVE_VOICE_CATALOG` export filters out `active !== false`. `filterVoices()`, `VOICE_CATEGORIES`, `VOICE_ACCENTS`, `VOICE_USE_CASES` all use active-only.
  - `findFallbackVoice(id)`: same-category + same-accent + same-gender → same-category + same-gender → any in same-category. Used in dashboard init + generation pipeline.
  - `isVoiceActive(id)`: returns false only for explicitly retired voices.
  - Generation pipeline (`lib/providers/elevenlabs-voice.ts`): auto-fallback if user's saved voice is retired.
  - `voice_preview` analytics event: tracked on every preview (cached + fresh) via `POST /api/analytics/track` pattern → `AnalyticsEvent` table. Metadata: voiceId, voiceName, category, tier, accent, gender, speed, cached.
  - Dashboard shows "✨ Refreshed voice library — discover updated selections" note + fallback notification if user's saved voice was swapped.
- **ENGLISH-ONLY LAUNCH (this turn) — Bug #1 fix**: foreign voices (German "Sissi" female-f-04, Japanese "Yuki", Jordanian "Farah") were playing samples in their native language. ROOT CAUSE: (a) both TTS generation routes lacked `language_code`; (b) the picker's `playSample()` fallback played ElevenLabs' canned `previewUrl` mp3s, which are recorded in each voice's NATIVE language. FIX: forced `language_code: 'en'` on ALL generation calls — added top-level in `app/api/voices/preview/route.ts` (~L77) AND `lib/providers/elevenlabs-voice.ts` `/with-timestamps` body (~L134). `voice-browser.tsx` `generatePreview()` ALWAYS synthesizes English (uses script line or `DEFAULT_PREVIEW_PHRASE`), never plays canned foreign sample (playSample only on error). `language_code:'en'` is SAFE on `eleven_multilingual_v2` (default tier) — returns 200, NOT an error (verified empirically).
  - **AUDIT RESULT**: tested all 151 active voices with forced English — 151/151 pass `works_in_english` AND `speed_test_pass`. ZERO retired. Final active count stays 151. CSV at `/home/ubuntu/voice_audit.csv` (cols: voiceId,name,category,accent,language_detected["en (forced)"],works_in_english,speed_test_pass,dur_slow_s,dur_fast_s,notes).
  - Accent variety PRESERVED as a feature: German/Japanese-accented English etc. are intentional, not bugs. Phase 7 = true multilingual (future).
- **DASHBOARD PLAY BUTTON FIX (turn 2 — the REAL fix)**: Previous turn's fix only modified `components/voice-browser.tsx` (hidden behind "Advanced Voice Controls" toggle — most users never see it). The VISIBLE voice cards in the dashboard use `toggleVoicePreview()` in `app/dashboard/page.tsx`, which was a completely different code path that directly played canned `previewUrl` mp3s. FIX: rewrote `toggleVoicePreview()` to:
  - Call `/api/voices/preview` with English text + speed (same as VoiceBrowser).
  - Cache audio by `${id}__${speed}` (not just `id`).
  - Show `Loader2` spinner on card during generation.
  - Handle custom (uploaded) voices separately (play their own audio directly, no ElevenLabs TTS).
  - Use `CATEGORY_TEST_PHRASES[category]` as preview text when no user prompt typed.
  - Import `CATEGORY_TEST_PHRASES`, `speedValue` from voice-catalog.
  - Added `previewLoadingId` state for loading spinner.
  - Browser-verified on LIVE manifestreelai.com: Farah (slow=41.1kB, normal=31.9kB, fast=26.0kB), Sissi (48.2kB), Koraly (49.0kB) — all produce unique English audio via API, not canned foreign samples.
  - LESSON LEARNED: Dashboard has TWO voice UI layers — main cards (always visible, own play logic) and VoiceBrowser (hidden, toggled by "Advanced Voice Controls"). Must fix BOTH when changing voice preview behavior.
- Music Vol. 2: per-track BPM/mood override doc coming. CSV format expected.
